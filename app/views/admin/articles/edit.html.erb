<h1>記事編集</h1>

<div style="background-color: var(--surface-color); padding: 1.5rem; border-radius: 0.375rem; border: 1px solid var(--border-color); margin-bottom: 2rem;">
  <%= render "form", article: @article, tag_names: @tag_names %>
</div>
